# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local

# Rust
/target/

# mdbook output
docs/book/book/

# Runtime artifacts (agent locks, session state)
.runtime/

# Gas Town (added by gt)
.claude/
.logs/
__pycache__/
state.json
CLAUDE.local.md
search-debug.json

# Session planning scratch (planning-with-files skill) — never committed:
# they carry crew names, work-item ids and host paths, and this repo is public.
task_plan.md
findings.md
progress.md

# Yupana verdict-signing identity: a PKCS#8 PRIVATE KEY, generated on demand by
# the verifier tests. Never committable — a `git add -A` after a test run put one
# in a public branch once (2026-09-05).
.quipu/
