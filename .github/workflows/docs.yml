name: Docs

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Publishes the mdBook to the `gh-pages` branch (contents:write), which GitHub
# Pages then serves. This avoids the official Pages deploy path, which needs a
# `pages: write` token this repo's integration doesn't grant. One-time setup:
# Settings → Pages → "Deploy from a branch" → gh-pages / (root).
permissions:
  contents: read

concurrency:
  group: docs-pages
  cancel-in-progress: false

jobs:
  relevant-changes:
    name: Relevant changes
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.paths.outputs.changed }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0
      - id: paths
        uses: ./.github/actions/path-gate
        with:
          base: ${{ github.event.pull_request.base.sha || github.event.before || github.sha }}
          head: ${{ github.event.pull_request.head.sha || github.sha }}
          paths: |
            docs/book/**
            .github/workflows/docs.yml
            .github/actions/path-gate/**

  build-deploy:
    name: Build relevant docs
    needs: relevant-changes
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      # An exact version, not a range: "^0.4" re-resolved at every run, so the
      # binary that ran with this job's write token could change without a
      # commit here. --locked builds from mdbook's published Cargo.lock, and
      # cargo checks every crate against the registry index checksum. Bump the
      # version in a reviewed change.
      - name: Install mdbook
        if: needs.relevant-changes.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        run: cargo install mdbook --version "=0.4.52" --locked
      - name: Build book
        if: needs.relevant-changes.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        run: mdbook build docs/book
      - name: Deploy to gh-pages
        if: >-
          (github.event_name == 'push' && needs.relevant-changes.outputs.changed == 'true') ||
          github.event_name == 'workflow_dispatch'
        uses: peaceiris/actions-gh-pages@84c30a85c19949d7eee79c4ff27748b70285e453 # v4.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/book/book
          publish_branch: gh-pages
